import SectionHeading from "./SectionHeading";

const Faqs = () => {
  return (
    <section id="faqs" className="flex justify-center py-[120px] bg-[#F8F9FC]">
      <SectionHeading heading={"FAQs"} />
    </section>
  );
};

// I don’t have logo and branding for my company yet, can I still submit a project?
// Absolutely! We can help you creating a unique, premium logo design for your new site. You can include this add on when submitting a project with us.

export default Faqs;
