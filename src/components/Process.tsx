const Process = () => {
  // Design process, building website, deployment
  // Design process - send mockups, if client doesn't like something we have unlimited revisions
  return <section>Process</section>;
};

export default Process;
